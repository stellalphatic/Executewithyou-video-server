
import React from 'react';

// User requested to remove extra particles for a cleaner look.
// Returning null effectively removes them from the scene.

export const ParticleDust = React.forwardRef((props, ref) => {
  return null;
});
ParticleDust.displayName = 'ParticleDust';
